{
  "name": "fast-jev-compaction",
  "version": "0.4.0",
  "description": "Verbatim Jev-guided compaction for Claude Code sessions.",
  "license": "MIT",
  "userConfig": {
    "apiKey": {
      "type": "string",
      "title": "TypeSafe API key",
      "description": "TypeSafe API key. Leave unset to use TYPESAFE_API_KEY.",
      "sensitive": true
    },
    "keepThreshold": {
      "type": "number",
      "title": "Keep threshold",
      "description": "Minimum Jev probability that a tool call or tool result is still needed; below it the item is removed.",
      "default": 0.5
    },
    "preserveRecentMessages": {
      "type": "number",
      "title": "Recent messages to preserve",
      "description": "Number of newest messages pinned from compaction.",
      "default": 6
    },
    "compactAtPercent": {
      "type": "number",
      "title": "Compaction percentage",
      "description": "Context percentage at which turn.complete requests compaction.",
      "default": 60
    },
    "minReductionRatio": {
      "type": "number",
      "title": "Minimum reduction ratio",
      "description": "Minimum estimated character reduction required to replace history.",
      "default": 0.25
    },
    "archiveResults": {
      "type": "boolean",
      "title": "Save dropped tool results",
      "description": "Write tool results dropped by compaction to .claude/fast-jev-compaction/ and cite the file in their marker, so they can be read back.",
      "default": true
    },
    "maxStateTokens": {
      "type": "number",
      "title": "Maximum Jev state tokens",
      "description": "Estimated token budget for the conversation state sent with every Jev request.",
      "default": 25000
    },
    "maxRequestTokens": {
      "type": "number",
      "title": "Maximum Jev request tokens",
      "description": "Estimated token budget for state plus questions in one Jev request.",
      "default": 30000
    },
    "truncateHeadChars": {
      "type": "number",
      "title": "Truncated tool result head",
      "description": "Characters of a tool result kept when Jev decides its full output is no longer needed; 0 keeps only the note.",
      "default": 300
    },
    "bashOutput": {
      "type": "boolean",
      "title": "Trim Bash output",
      "description": "Trim long Bash output with Jev before the model sees it",
      "default": false
    },
    "bashOutputMinChars": {
      "type": "number",
      "title": "Minimum Bash output characters",
      "description": "Bash outputs at or below this length pass through untouched.",
      "default": 4000
    },
    "bashOutputChunkLines": {
      "type": "number",
      "title": "Bash output chunk lines",
      "description": "Number of Bash output lines grouped into each Jev chunk.",
      "default": 20
    },
    "model": {
      "type": "string",
      "title": "Jev model",
      "description": "TypeSafe Jev model name.",
      "default": "jev-latest"
    }
  }
}
